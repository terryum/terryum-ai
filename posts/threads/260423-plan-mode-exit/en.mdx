---
title: "Handling an existing plan file when re-entering Plan Mode"
summary: "Summary of a ChatGPT thread on when Claude Code should overwrite vs. extend the existing plan file on re-entry."
---

When re-entering Plan Mode in Claude Code, an unrelated new request should overwrite the existing plan file, while a continuation should edit sections in place. The decision hinges on whether the new request touches the same code area and aims at the same final deliverable.

This is a scaffold post and should be replaced by a real published thread later.
